.topbar {
    position: sticky;
    top: 0;
    left: 0;

    background: rgba(0, 0, 0, 0.4);
    border-bottom: solid 1px rgba(255, 255, 255, 0.2);

    width: 100%;
    height: 75px;

    display: flex;
    flex-wrap: nowrap;
    flex-direction: row;


    /* 1. Un fond semi-transparent (ici un blanc à 20% d'opacité) */
    //background-color: rgba(255, 255, 255, 0.2);

    /* 2. L'effet de flou qui s'applique sur ce qui est DERRIÈRE la div */
    backdrop-filter: blur(12px);

    /* 3. Sécurité pour les anciens navigateurs Safari */
    -webkit-backdrop-filter: blur(12px);

    > .topbar-left, > .topbar-center , > .topbar-right {
        flex: 1;
        display: flex;
        align-items: center;
        padding: 20px;
    }

    .topbar-left {
        text-align: left;
        justify-content: left;
    }

    /* Hide on mobile */
    .topbar-center {
        display: none;
        text-align: center;
        justify-content: center;
    }

    .topbar-right {
        text-align: right;
        justify-content: right;
    }

    /* Display only on desktop */
    @media (--media-lg) {
        .topbar-center { display: flex; }
    }

}

